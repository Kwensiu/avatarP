# 头像工具

一个纯前端头像裁切工具，用来选择图片、调整位置、预览效果并导出头像。

## 功能

- 本地选择图片，不上传服务器
- 拖动调整头像位置
- 支持缩放和旋转
- 预览常见平台头像效果
- 导出 PNG / JPG / WebP

## 使用

直接用浏览器打开 `index.html` 即可。

也可以启动一个本地静态服务：

```bash
python -m http.server 8000
```

然后访问：

```text
http://localhost:8000
```

## 部署

这是一个单文件静态页面，可以直接部署到 GitHub Pages、Netlify、Vercel 或任意静态网站服务。
